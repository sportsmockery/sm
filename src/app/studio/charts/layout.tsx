import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Sports Mockery | Charts',
}

export default function StudioChartsLayout({ children }: { children: React.ReactNode }) {
  return children
}
