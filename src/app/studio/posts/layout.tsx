import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Sports Mockery | Posts',
}

export default function StudioPostsLayout({ children }: { children: React.ReactNode }) {
  return children
}
