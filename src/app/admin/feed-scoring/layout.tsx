import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Feed Scoring',
}

export default function FeedScoringLayout({ children }: { children: React.ReactNode }) {
  return children
}
