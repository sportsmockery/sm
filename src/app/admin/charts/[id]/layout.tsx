import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Edit Chart',
}

export default function EditChartLayout({ children }: { children: React.ReactNode }) {
  return children
}
